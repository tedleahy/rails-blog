<h1><%= @post.title %></h1>

<div class="mb-2">
  <%= link_to 'Edit', edit_post_path(@post) %>
  <%= link_to 'Delete', post_path(@post), data: {
    turbo_method: :delete,
    turbo_confirm: 'Are you sure?',
  } %>
</div>

<p><%= @post.body %></p>
